when: 2024-12-19T11:45:00
where:
  - /Volumes/so/github.com/supersongssr/NPanel/app/Http/Controllers/Api/PingController.php
  - /Volumes/so/github.com/supersongssr/NPanel/routes/api.php
why: 添加获取可用节点的API接口，方便后端系统获取一个可用的节点ID用于上传配置
how:
  1. 在PingController中添加getNewNode方法
  2. 参考现有的simpleApiTools中的节点查询逻辑
  3. 查找ID>99、心跳超过7天、状态为0（维护中）的节点
  4. 返回node.id和node.v2_host信息
  5. 添加API路由映射
do:
  - 创建getNewNode方法，验证API_TOKEN
  - 查询符合条件的可用节点
  - 更新节点心跳时间
  - 返回节点ID和v2_host信息
  - 在routes/api.php中添加GET路由
  - 创建测试脚本验证功能
must:
  1. API必须验证token，防止滥用
  2. 返回的数据必须包含node_id和v2_host
  3. 查询条件：ID>99，heartbeat_at超过7天，status=0
  4. 获取节点后必须更新heartbeat_at时间戳
  5. 如果没有可用节点，返回node-empty错误
  6. 必须在远程服务器测试通过